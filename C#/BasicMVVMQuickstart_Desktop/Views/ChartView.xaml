<UserControl x:Class="BasicMVVMQuickstart_Desktop.Views.ChartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="300">
    <Grid x:Name="LayoutRoot">
        <GroupBox Header="PnL Chart" Grid.Column="0" Grid.Row="1" Height="Auto" Margin="10,5,10,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="25"/>
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Button Grid.Row="0" Command="{Binding SubmitCommand}" Width="150" Margin="2,0,2,2" >Generate PnL chart to file</Button>
                <Image Grid.Row="1" Source="../Images/PnLchart.jpg" Stretch="Fill" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,0,0,25"/>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
