<UserControl x:Class="TradingUI.Views.MarketView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500"
             d:DesignWidth="400">
    <Grid x:Name="LayoutRoot">
        <GroupBox Header="Market" Grid.Column="1" Grid.Row="0" Grid.ColumnSpan="1" Height="Auto" Margin="0,10,10,10">
            <Grid Name="gridMarket">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition Height="24" />
                </Grid.RowDefinitions>
                <Label Name="MarketPrice" Content="Strike: " Grid.Row="0" />
                <Label Name="MarketPriceValue"  Content="{Binding Market.Price}" Grid.Row="0" />
                <Label Name="MarketVol" Content="Volatility: " Grid.Row="0" HorizontalAlignment="Center" />
                <Label Name="MarketVolValue" Content="{Binding Market.Vol}" Grid.Row="0" HorizontalAlignment="Center" />
                <Label Name="MarketDate" Content="Date: " Grid.Row="0" HorizontalAlignment="Right" />
                <Label Name="MarketDateValue" Content="{Binding Market.BusinessDate, StringFormat='yyyy-MM-dd'}" Grid.Row="0" HorizontalAlignment="Right" />
                <Button Command="{Binding ConfigureMarketCommand}" Grid.Row="0" Grid.Column="0" Height="25" Width="110" Content="Configure Market" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,25,0,0"></Button>
                <Image Source="../Images/marketChart.jpg" Stretch="Fill" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,55,0,0"/>
                <DataGrid Name="optionDataGrid" 
                          ItemsSource="{Binding optionList}" SelectedItem="{Binding selectedOption}"
                          AutoGenerateColumns="False"  IsReadOnly="True" Grid.Row="1" Margin="10,10,10,10">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="25*" />
                        <DataGridTextColumn  Header="Maturity" Binding="{Binding Maturity, StringFormat='yyyy-MM-dd'}" Width="25*" />
                        <DataGridTextColumn Header="Price" Binding="{Binding Price}" Width="25*" />
                    </DataGrid.Columns>
                </DataGrid>
                <Button Grid.Row="3" Command="{Binding AddOptionCommand}" Width="140" Margin="-160,0,2,0" HorizontalAlignment="Center">Add new option</Button>
                <Button Grid.Row="3" Command="{Binding TradeOptionCommand}" Width="140" Margin="160,0,20,0" HorizontalAlignment="Center">Trade selected option</Button>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
